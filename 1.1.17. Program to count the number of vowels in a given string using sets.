def vowel_count(str):
	count = 0
	vowel = set("aeiouAEIOU")
	
	# Write your code here to count the vowels
	for ch in str.lower():
		if ch in vowel:
			count += 1
	print (count)
str = input()
vowel_count(str)
